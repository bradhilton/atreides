[project]
name = "atreides"
version = "0.1.0"
description = "Atreides Python SDK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=6.29.5",
    "prisma>=0.15.0",
    "skypilot[lambda]>=0.7.0",
]

[tool.uv]
dev-dependencies = [
    "vllm>=0.6.6.post1",
]
